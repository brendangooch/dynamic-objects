/**
 * a sequence of timed callback functions that are called as the sequence progresses at the specified point in the sequence
 */

import { BaseDynamicObject, DynamicUnit } from "../index.js";

export class DynamicSequence {

    //

}